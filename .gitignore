# Local dev / scratch — never deploy to the live site
node_modules/
.netlify/
.claude/worktrees/
.claire/
BRAND_CONTEXT.md
assets/video/_originals/
generate_video.py

# Secrets — this repo is PUBLIC (GitHub Pages); nothing below may ever land in it
.env
.env.*
.dev.vars
*.pem
*credentials*.json
.wrangler/
