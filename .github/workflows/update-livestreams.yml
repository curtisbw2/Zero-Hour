name: Update Livestreams

on:
  schedule:
    # Daily 13:00 UTC (~8-9am ET), catches the previous day's streams
    - cron: '0 13 * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # sitemap lastmod needs git history

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Pull new livestreams from YouTube RSS
        run: node scripts/update-livestreams.js

      - name: Regenerate sitemap
        run: node scripts/generate-sitemap.js

      - name: Commit and push if changed
        id: commit
        run: |
          git config user.name "zhg-streams-bot"
          git config user.email "actions@users.noreply.github.com"
          git add livestreams.html earnings.html scripts/livestreams.json sitemap.xml
          if git diff --cached --quiet; then
            echo "No changes"
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            git commit -m "Auto-update livestreams" && git push
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Ping IndexNow with changed pages
        if: steps.commit.outputs.changed == 'true'
        run: node scripts/indexnow-ping.js
